# Frontend Environment Variables (Example)
# All environment variables must start with "VITE_"

# VITE_API_URL=https://api.example.com
# VITE_APP_NAME=HandSpeak

# Note: Since this project runs locally (Edge), 
# no mandatory API keys or URLs are required at the moment.
